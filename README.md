# Task 1 — Data Cleaning

## Explaining
Reads input CSV `task1_cleaning/data/input.csv`, normalizes fields, deletes duplicates,
saves clean file with JSON-report.

## Requirements
- Python 3.11+

## Installing
```bash
python -m venv .venv
# Windows: .venv\Scripts\activate
# macOS/Linux: source .venv/bin/activate
pip install -r requirements.txt
```

## Run
```bash
cd task1_cleaning
python main.py
```
